To run the file make sure the following files are in the same folder:

1. ‘data_training_2.py’ , this is the file that has the code
2. data.csv, this is the data file.
3. header.txt, these are the header to the data file

Now, go to the folder that has these files, then type the following in the Terminal:

python data_training_2.py


Now, you should see the following results for each machine learning model
1. Accuracy.
2. Precision.
3. Recall.
